name: 🚢 Deploy production

on:
  workflow_dispatch:

jobs:
  build:
    name: Build and push Docker image
    uses: ./.github/workflows/reusable-build-and-push-docker-image.yml
    secrets: inherit
    permissions:
      contents: read
      id-token: write
      packages: write

  pre-deploy:
    environment: production
    name: Pre-deploy to production
    needs: build
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Pre-deploy
        env:
          IMAGE_VERSION: ${{ needs.build.outputs.image-version }}
        # language=sh
        run: |
          echo "::notice::Pre-deploying ${IMAGE_VERSION} to production"

  deploy:
    environment:
      name: production
      url: https://app.example.com
    name: Deploy image ${{ needs.build.outputs.image-version }} to production
    needs:
      - build
      - pre-deploy
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Deploy image
        env:
          IMAGE_VERSION: ${{ needs.build.outputs.image-version }}
        # language=sh
        run: |
          echo "::notice::Deploying ${IMAGE_VERSION} to production"
